/* exported StoreData */

class StoreData {
    constructor(storeLocation, min, max, avg) {
        this.storeLocation = storeLocation;
        this.min = min;
        this.max = max;
        this.avg = avg;
    }
}
// create a header

// initialize variables to keep track of hourly totals

// add all the locations to a table (loop through list of locations)



// create a footer (include the hourly totals across all locations)